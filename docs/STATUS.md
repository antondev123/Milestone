# STATUS

Updated: 2026-09-18 (chapter 2 verbatim; voice picker in Settings; stage demo mode + 2.5 verbatim; intent-aware answers, ask on Sonnet 5; open-ended trips, picker removed, sections and milestones spoken on the road; demo question set review; Dial ring notched per section, Where-block dots removed; milestones on the summary and Progress; session log + diagnostics pages; Study checkpoint layout and leg/section navigation; Study desktop scaling + Phone preview; Study desktop column tidy; Study assistant mic dictation; play button fix for Dial and Study; app renamed to Milestone)

## Done
- **Chapter 2 is verbatim** (2.2–2.7, 31 legs, 61 questions): `verbatim/c02.json` now covers the whole chapter, each leg cut from the section markdown on the book's own headings, questions in the book's terms with all of the book's concept checks used verbatim (`source: "book"`). Coverage of body prose: 2.2 91%, 2.3 98%, 2.4 94%, 2.5 94%, 2.6 82% (the logical-fallacies table and technique table are column-scrambled PDF text and cannot be spoken; excluded), 2.7 100%. `scripts/verbatim-chapter.ts check <spec.json>` is a new dry run (word counts, blocks, first/last words of every leg, hygiene, coverage per section) and an empty `end` marker now means "to the end of the section". Source defects found on the way, left as they are in `source/` and worked around with markers: `c02-s04.md` has its Concept Check heading as plain text `concept Check` (so those four questions sat in the body; used them anyway), `c02-s06.md` has "These may sound, but consider" with a word missing from the parse, `c02-s07.md` has a figure caption cut at "Credit (Mr.". No localised examples remain anywhere in chapters 1–2. Grader spot-check through the live tool route: strong paraphrases accepted, weak answers hinted and retried, spoken MCQ picks matched. The seed now marks 11 of 31 chapter-2 legs done; docs/DEMO.md numbers updated.
- **Voice picker** (`/settings`, linked from the Resume header and Progress top bar): eight of ElevenLabs' trending English library voices, each with a play/stop sample (static mp3s in `public/voices/`, no credits, no runtime API call). The pick is stored as `Progress.voiceId` (**schema: new optional field**) and applies to both Study read-aloud (`/api/tts?…&voice=<id>`; the voice is part of the disk cache key, and the URL param keeps the browser cache honest) and Listen (`overrides.tts.voiceId` on `startSession`). No pick → `ELEVENLABS_VOICE_ID` (Alice) for TTS and the dashboard voice for the agent, so the chapter‑1 TTS cache stays warm. `npm run demo:reset` / `demo:stage` keep the pick. Offline: `npm run voices:sync` (`scripts/voices.ts`) refreshes the list, adds the voices to the account (library voices are unusable until added; 8 of 30 slots) and downloads the samples. **The agent must allow the voice override: run `npm run agent:configure` after merging** (already run against the live agent). Verified with live keys: samples play and switch, pick survives reload, Study synthesised blocks 0–1 with Alistair, a text-only Listen session recorded `tts.voice_id` on the ElevenLabs conversation, unknown ids are rejected and fall back to Alice.
- **Stage demo mode** (docs/DEMO.md is rewritten around it): `npm run demo:stage` wipes progress, seeds the position (chapter 1 and 2.1–2.4 done) and arms a fixed Listen trip (`Progress.demo`, **schema: new optional field**, see docs/SCHEMA.md). Trips are open-ended everywhere else; with the demo armed, the next Listen trip (`actionStartSession` in voice mode → `src/lib/demo.ts`) is exactly 2.5 parts 1–2, the only trip with a non-empty `ActiveTrip.segmentIds`, and `cursor.serveNext` ends it after the last planned leg: ~80 s of the book, an open check graded by Claude, an MCQ, ~2 min more with room for a live `ask`, one open check, then the summary. Runs **once**: trip end deletes the flag; browsing elsewhere first (cursor off the first demo leg) disarms it, so nobody gets yanked back. A reload before the end restarts the identical run. Verified end to end through the API with the live key (vague open answer rejected and retried, paraphrases accepted, spoken MCQ pick "the new ground one" matched locally, `ask` "is that the same as the sunk cost fallacy?" answered from the part's deeper note, "Back to Escalation of commitment" resume, self-ending trip).
- **Section 2.5 is verbatim** (book text, no localisation): `scripts/verbatim-chapter.ts` now builds every `verbatim/cNN.json` spec, slicing from `verbatim/cNN.txt` when it exists (chapter 1) or from the section's `source/` markdown (2.5; no PDF on the machine, and 2.5's parse has no dropped lines). Seven legs cut on the book's headings (bounded rationality 80 s, escalation of commitment 2 min, time constraints and uncertainty, similar-to-me and confirmation bias, why beliefs resist change, conflict, Nadella), questions authored in the book's terms with its three concept checks marked `source: "book"`. Chapter 1 output is byte-identical. Validator warn floor for `durationSec` lowered to 60 s.
- **Streak milestone and the summary hero**: new `streak-1` "Day one of your streak" (first in display order). Streaks only resolve at trip end, so the closing line speaks them ("Milestone: day one of your streak."); the others are already spoken on the road. The summary's milestone card is the gold hero above the stats. Progress shows a position line from `segmentsCompleted` ("1 chapter finished, 14 legs done on the map") so a seeded app reads as progress without fabricated trips. `cursor.commit` no longer clobbers the resume pointer after "Part done", so the summary's "Next leg picks up at" names the next part, not the finished one. Closing sentence starts capitalised ("Two parts").
- **Trips are open-ended; the trip-length screen is gone.** Read and Listen start a trip the moment they open (`POST /api/session {mode}`, no minutes; `?carry=1` keeps a running trip and falls back to a fresh start on 404). The planner only picks the start position; the cursor walks the course in order until you end the trip (End trip / hold End / "I'm done") or the course runs out. Instead of stopping, the tutor tells you what you finished and carries on: section and chapter ends are named aloud ("Section two point five, Title, done. Next, …"; "Chapter two, Title, done."), and **milestones are spoken the moment they are earned** (part done, quiz done, first question: "Milestone: First hands-free trip.") via `earnNow` in `src/lib/milestones.ts`, recorded on `ActiveTrip.milestones` and merged into the summary (streaks still resolve at trip end). Listen plays a rising earcon for a section/chapter end and a short fanfare plus the label in the state word for a milestone. **Schema:** `ActiveTrip.milestones?`, `ToolReply.milestones?`, `Plan` loses `estMinutes`, `TripSummary.minutes` is now elapsed (was the pick), `ActiveTrip.minutes` is 0. `src/components/TripPicker.tsx` deleted. The typed-command box that `?text=1` showed on the Dial is gone too (the screen is hands-free; Read mode is the typed fallback), and skipping a question no longer leaves the Dial stuck on "Answer out loud" (a `more: true` reply clears the open question so auto-continue resumes). The Listen credit guard is a flat 2 h (`HARD_STOP_MS`) and the agent's `max_duration_seconds` is 7200: **run `npm run agent:configure` after merging.**
- **Milestones** (light gamification): named stops earned from logged data only: First trip, First hands-free trip, First question from the road, Clean run (3+ checks all right first time), 3-/5-day streak, Chapter n finished, Chapter n quiz passed (70%+). Each needs this trip to have caused it, so seeded progress never earns one; awarded once. Rules in `src/lib/milestones.ts`; stored as `TripSummary.milestones` (**schema: new optional field**). Shown as a card on the summary and a list on Progress, and spoken on the road as they happen (see above).
- **Intent-aware answers**: whatever the learner says while a checkpoint or quiz question is open goes through `answer`, and only a real attempt is graded. Before: "can you repeat the question", "go to chapter one", "hold on", "skip this one" and "I don't know" were all graded wrong, the model answer was leaked in the feedback, and then "Try once more"; "what does escalation of commitment mean?" was graded *correct* because the option name was in the question. Now (`actionAnswer` in `src/lib/actions.ts`): unmistakable commands are dispatched with no model call (`src/lib/intent.ts`: repeat, skip, hold on, where am I, go to…, I don't know), the grader classifies the rest in the same Sonnet call it grades with (`intent`: answer / question / command / giveup), a question is answered from the book via `ask` ("…Say continue to get back to the question."), a command goes to `goto`, a give-up reveals the answer and moves on, and none of those record a checkpoint or burn the retry. **A wrong first try now gets a hint in every mode** (the grader is told not to name the answer; `mcqHint` for MCQ picks); the second try or a give-up reveals it. "Skip" during a checkpoint skips one question, not the whole checkpoint. "Go back" after a goto from an open question returns to the question (`returnStack` carries `phase`/`qIdx`). "Repeat" after a detour answer re-reads the question. Text mode sends every typed line to `answer` while a question is open (typing "C" during an MCQ used to go to `ask`); Study shows a routed reply as a note under the question. Voice: the agent prompt and the `answer` tool description now say to use ask/goto/explain for non-answers — **run `npm run agent:configure` after merging** — and the server routing catches whatever Gemini still sends as an answer. Additive fields: `GradeResponse.intent`, `ToolReply.intent`, `Cursor.returnStack[].phase/qIdx` (`src/types/lesson.ts`). Verified with live keys on the 2.5 MCQ and open question via curl, Read quietly and Study in the browser.
- **`ask` runs on Sonnet 5** (`ANTHROPIC_ASK_MODEL` default; `effort: low`, timeout 10 s, same three cached prompt blocks). Haiku is still one env var away if latency bites in rehearsal.
- **Demo question set reviewed and fixed**: every 2.5 checkpoint and the chapter 1 quiz checked against the book text and run through `gradeAnswer` with strong / spoken / partial / wrong answers. 2.5: the MCQ (taxi-minibus escalation of commitment) now comes first, so Beat 1 is a tap; g1/q3 (programmed vs nonprogrammed, a 2.2 idea) became time pressure → heuristics; throwaway distractors ("It has no effect on the brain", satisficing/groupthink before 2.6/2.7) replaced with concepts already taught; Nadella question asks for the mechanism. Chapter 1 quiz q5 ("occupation rather than a profession") was not taught anywhere in the chapter and its answer was invented; now "How does a manager's job change with the department or function?" from the book's management-by-function passage. q6 rubric asks for any two points, not all three. **MCQ answers were option A in most sections**: `scripts/mcq-order.ts` sets the answer slot per section (hash-seeded, stable, idempotent), used by ingest and `course:verbatim`; `node scripts/ingest.ts pom --chapters 1-3 --reshuffle` reorders existing files with no Claude call (already applied to chapters 1–3). `npm run validate` warns when all MCQ answers in a section share a letter or an answer is much longer than the rest; ingest prompts ask for plausible, similar-length distractors and quiz questions the chapter answers. **Grader**: a spoken MCQ pick ("it's escalation of commitment") matches locally instead of needing Claude; without a key or on an API error, grading used to throw ("Something went wrong there") and now falls back to keywords (word stems, short feedback). `tsconfig` gains `allowImportingTsExtensions` so scripts can share a helper.
- **Study checkpoint and navigation**: the inline checkpoint now scrolls under the top bar with a margin, the feedback box and "Next question" scroll clear of the pinned strip, the assistant FAB hides while a question is open, the section fades in, and only one `Checkpoint` mounts (phone inline *or* desktop column, chosen with `useMediaQuery`; before, both mounted and each POSTed `check`). "Next leg" left the checkpoint: a `LegNav` footer after the passage always offers Previous / Next leg (outlined; Next turns gold once the leg's checkpoint is done, so there is one primary per screen) and an "All chapters" link. The top-bar title ("Leg 1 of 8 · 1.1") is a button that opens a `SectionPicker` sheet (Text mode's Jump to… list with You are here / Done, chapter quiz links, All chapters) that posts `goto`. Verified on phone and desktop in the browser.
- **Study desktop scaling + Phone preview**: the ≥1024px layout sat small on big monitors (1100px grid, px type). `StudyLayout` now `zoom`s the frame with the viewport (`width / 1280`, clamped 1–1.8): 1280 is 1:1, 1920 is 1.5×, 2560 is 1.8×. `zoom` takes part in layout, so the sticky strip, the scrolling aside and the `lg` media queries are untouched; phones never scale. A small phone icon at the right of the Ask / Check row (desktop only) opens the same route in a 390×844 iframe (`?frame=1`) so the real phone layout — breakpoints, sheet, FAB — can be checked from a PC; Escape or × closes, the choice survives a reload (`localStorage` `study:phone`). Verified at 1280×900, 1920×1080, 2560×1440 and 375×812.
- **Study desktop column tidy (post #15)**: on short windows the assistant column clipped the empty-state line under the quoted sentence, hid the chips and drew stray scrollbars. Cause: nested `overflow-y-auto` flex regions plus a `-mx-1` chip row. Now only the thread scrolls (min 120px), quote/chips/composer sit below it at natural height, chips swipe without a scrollbar, and the aside itself scrolls as a last resort. The "Reading now" card is gone on desktop (the strip, the page highlight and the quoted context already show that sentence). The checkpoint gets an `inline` prop for the column (no top rule, smaller titles); the phone flow is unchanged. Verified at 1280×560, 1280×900 and 375×812.
- **Session log & diagnostics** (README "Diagnostics"): every trip is recorded to SQLite (`node:sqlite`, no dependency) under `data/progress/logs/`: tool calls with request/reply/latency, live SDK transcript, ElevenLabs' own transcript + tool calls + per-turn Gemini usage + credits pulled after the call, Claude/TTS usage with tokens and $, browser errors, pause/mute/interruption markers, the learner's mic (Listen mode, `MediaRecorder` → `audio/<trip>/mic.webm`), Study dictation clips (`dictation-<n>.<ext>` + Scribe text) and the full call recording. URL-only pages `/admin/sessions` and `/admin/sessions/<tripId>`, JSON at `/api/log/sessions[/<id>]`, `npm run sessions` CLI. Off localhost set `ADMIN_SECRET` and use `?key=`. Verified on localhost: Read trip (ask + open grade logged with tokens/$), `/learn/voice?text=1` trip (conv id attached, 22-turn ElevenLabs transcript synced, 151 credits), Study read-aloud (TTS chars/credits), injected browser error → `client_error` row, 401/403 gate on the standalone build, `demo:reset` leaves the log alone.
- **App renamed to Milestone**: browser tab title and the Resume header. "Carry" stays as the name of the design palette and the `components/carry` folder.
- Task 1: docs/OUTLINE.md
- Task 2: docs/SCHEMA.md + src/types/lesson.ts (added `ActiveTrip` on Progress)
- Task 3: Next.js 16 + TS + Tailwind 4 scaffold, README, .env.example, npm scripts (`dev`, `ingest`, `demo:reset`, `typecheck`). Runs on Node 24 with no tsx.
- Task 4: scripts/ingest.ts (Claude Sonnet, structured JSON output, sanity checks). Sample course raw + a hand-authored lesson.json (4 segments, 9 questions) so the app runs before ingest. **Ingest run live with Sonnet 5: 4 segments, 12 questions, ~20 min. The committed lesson.json is now the ingested one.**
- Task 5: ElevenLabs wiring: `VoiceAgent` component (ConversationProvider + client tools), `/api/tools/[tool]` webhook route (same four tools), docs/ELEVENLABS.md with prompt, first message, tool schemas, Flash TTS. Agent configured via `scripts/configure-agent.ts` (Gemini 3.6 Flash, Flash v2, 4 client tools). Live voice test pending.
- Task 6: Text mode, verified end to end in the browser (plan → read → MCQ → open answer rejected/accepted → complete → summary).
- Session planner (`src/lib/planner.ts`), progress store (JSON file + memory fallback), grader (MCQ local, open via Claude, keyword fallback without key).
- Trip summary page `/trip/[id]/summary`, verified.
- Trip end on Fly fixed: `end_trip` is idempotent (no 0-minute phantom trip on a second call), voice mode ends the session once through one guarded path, and the hop to the summary is a full navigation. Verified on localhost with a double-clicked End trip: one `end_trip` POST, one summary load, no console errors.
- **Trip ends itself when the course runs out** (superseded the planned-legs version, trips are open-ended now): `next` past the last segment of the course ends the trip server-side and replies `kind: "end"` with the "Trip done…" line and a `tripId`. Read quietly shows the line for 1.5 s then opens the summary; Listen speaks it then hands over after 4 s. A later `end_trip` returns the same trip.
- docs/DEMO.md first draft with the interruption beat.
- **Listen is a car-mode screen (the Dial).** No lesson text: Topic / Section (`WhereBlock`), a 220px pause/play dial with a topic-progress ring notched at each section boundary (`Dial`; replaced the section dots), a 96px mic button carrying listening / question / muted (`MicButton`), hold-to-end (`HoldButton`), a one-word state line, a 6px voice strip (`VoiceStrip`) and WebAudio earcons (`src/components/earcons.ts`). Pause = volume 0 + mic closed + `interrupted` + a "pause" turn; Continue re-reads the block ("repeat the question" if one is open). `Leg` now carries titles and section/part indices (`src/lib/view.ts`); no schema change. Verified end to end on `/learn/voice?text=1`: ready → connecting → reading → question (mic gold) → correct ("That's right") → pause/continue → goto (Where block + ring change) → hold-to-end → summary. Spec: docs/DESIGN.md §4.3 and the "Trip Car Mode" artifact.
- **Play button fix (post #11/#12)**: the Dial's progress-ring `<svg>` sat above the button and swallowed every tap (`pointer-events-none` added, button made `relative`). Study never left "Getting your place…" in `next dev`: Strict Mode's double effect run tripped the boot's cancel flag before the segment loaded (flag removed; the `booted` ref already keeps it to one run). The Next dev badge sat over the strip's play button (`devIndicators: false`). And `/api/tts` 503s without `ELEVENLABS_VOICE_ID`; the strip now shows that error in gold instead of muted text. **Everyone: add `ELEVENLABS_VOICE_ID` to `.env.local` and restart dev** (Study audio needs it).
- Fly.io deploy config: `fly.toml`, `Dockerfile` (Next standalone), `npm run deploy:fly`, `docs/DEPLOY.md`. One always-on machine in jnb with a volume for `data/progress`, so the file store works as on localhost. Standalone build verified locally (pages, `/api/session`, progress write, `demo-reset --seed`). Not yet pushed to Fly: needs `fly auth login` + `fly apps create` + volume, see DEPLOY.md.

- **Study mode** (`/learn/study`, third card on Resume, course-map section links): hands-on reader for when you are not travelling. The leg's verbatim text as a page; ElevenLabs read-aloud per block via `/api/tts` (with-timestamps, cached in `data/tts`) with the spoken word and sentence tracked on the page and mirrored in a pinned strip (play/pause, ±1 sentence, speed); tap a sentence to play from there; audio off = plain page. Assistant = the `ask` tool with the tapped sentence as context (bottom sheet on phones, right column at ≥1024px). Checkpoint = tap/type UI over the new `check` tool + existing `answer`. Reading `mark`s the cursor per block, so Listen mode says "Back to it." at the same block. Needs `ELEVENLABS_VOICE_ID` (see .env.example); without it the page works with audio off. Mockups and brief in `docs/mockups/study/` (`node docs/mockups/serve.mjs`).

## Verified with live keys (2026-09-18)
- Anthropic key + `claude-sonnet-5`: ingest and grading. Grader rejects vague answers, accepts paraphrase, handles spoken MCQ ("thirteen hundred rand"). ~3.5–4.5 s per grade; acceptable for voice, watch it in rehearsal.
- ElevenLabs key: valid, creator tier, 0 agents created yet.

- **Carry design applied** (docs/DESIGN.md, docs/CONTEXT.md): Resume `/` and Progress `/progress` are new; `/learn/text`, `/learn/voice`, `/course` and the trip summary are re-skinned with the same behaviour (Fraunces + Public Sans, Carry tokens, top bar with mode pill, route line per chapter, answer buttons, feedback box with "From Principles of Management by OpenStax, section X", lost-signal notice with retry, no emoji).
- **Chapter 1 is verbatim**: eight legs cut from the PDF text layer (`scripts/verbatim-chapter.ts`, `data/courses/pom/verbatim/`), 1/1/3/3 legs across 1.1–1.4, `model: "verbatim"`. Chapters 2–3 are still the ingested adaptations. Chapter 1 quiz unchanged.
- Seed is position-only (no fabricated trips, answers or streak).
- **Voice lessons start on their own**: the greeting no longer says "Say go"; `VoiceAgent.start()` arms auto-continue so the first block follows the greeting. Agent prompt (`scripts/configure-agent.ts`) updated to expect the synthetic "continue" as the start signal, so run `npm run agent:configure` after merging.
- **Mode switch keeps the trip**: tapping Listen instead / Read instead mid-trip opens the other mode with `?carry=1`, which calls `POST /api/session {carry: true}` (`actionCarryTrip`). Same trip id and clock; only `activeTrip.mode` changes ("Carrying on with your trip."). Arriving from the home screen starts a new trip.
- **Study mode MCQ is hint-first**: a first wrong tap gets a one-sentence Claude nudge (`mcqHint` in `src/lib/grader.ts`, same model as open grading, fixed line without a key) instead of the answer, and the missed option is struck out; the second miss reveals the answer as before. Voice and Text modes are unchanged (`hintFirst` is only set for `mode === "study"` in `cursor.answer`).
- `chunk.ts` sentence splitter no longer breaks on initials ("U.S."), so block boundaries hold in 1.3.

## In progress / needs a human
- Session log with a real mic: Listen trip in Chrome, then `/admin/sessions/<tripId>` should show a playable learner-mic recording and, ~30 s after the end, the ElevenLabs call recording (`eleven-call.mp3`). Both paths are untested with spoken audio.
- Demo questions with a live `ANTHROPIC_API_KEY`: the review ran without one (all `.env.local` files had it empty), so open answers were checked on the keyword fallback only. Before rehearsal, answer each 2.5 checkpoint and the chapter 1 quiz once in spoken style and confirm Claude accepts paraphrase and rejects vague answers.
- Dial screen with a real mic in Chrome: pause must actually silence the tutor (ducking is gated by `isPaused`), Continue re-reads the block, the mic outline goes gold while listening, earcons audible over the tutor. Squint test at arm's length.
- Voice mode mic test in Chrome per docs/ELEVENLABS.md §6: confirm the 700 ms auto-continue grace window feels right and barge-in mid-block re-reads the block. Chunked reading is now server-driven, so no prompt tuning for it.

## Not started
- **Chapter 3 verbatim**: same recipe as chapter 2 (`verbatim/c03.json`, one leg per book heading, cut from `source/c03/*.md`; run `scripts/verbatim-chapter.ts check` while authoring). Until then chapter 3 is the localised paraphrase.
- Stage demo with a real mic in Chrome: confirm the interrupt lands early in 2.5 part 2 block 1 (the re-read after "continue" is the whole block, ~60 s) and that the six-minute pacing in docs/DEMO.md holds.
- Study mode follow-ups: word-level resume (the cursor is block-level). Chapter 3 is an ingested paraphrase, so the "book as it is" promise holds for chapters 1–2 only until it is cut the same way.
- Listen dial in landscape (dash mount): Where block left, dial centre, mute/End right. Portrait only for now.
- First real `npm run deploy:fly` and a phone test over HTTPS (mic needs a secure origin).
- **parse-book.ts drops real prose**: every `> **` line is treated as a figure caption, but pdftotext glues body text onto some of them. Lost from `source/`: most of Decisional Roles (1.3) and the levels-of-management paragraph (1.4); the ingested 1.3/1.4 lessons filled the gap from the model, not the book. Chapter 1 now bypasses this; chapters 2+ need a parser fix and a re-ingest check.
- Resume card precision is block-level (~150 words): the cursor has no word offset, so the card shows the end of the last fully heard block, not the exact word.
- Summary polish: mastered/revisit section only shows once topics have ≥2 attempts.

## Cut
- Maps API
- Trip-length picker ("How long is this trip?"): trips run until you end them
- Slides ingestion (transcript + quiz.md only)
- Auth / Supabase (single demo user, JSON store)
- Multi-course catalogue (one course; the old `sample` course still loads with `COURSE_ID=sample`)

## Gotchas
- Stage demo: it fires only for a Listen trip while the cursor still stands at 2.5 part 1, and only once. Reading or studying past that point first, or an earlier demo run, disarms it; the trip then just carries on past part 2 like any open-ended trip → say "I'm done", run `npm run demo:stage`, reload. `npm run demo:reset -- --seed` does not arm it.
- Hosting: Fly (one machine + volume) keeps the file store. Vercel/Netlify would fall back to memory per instance and drift between requests; not used.
- Grading latency: Sonnet 5 at low effort is ~4 s; the 2.5 s "One sec." filler covers it. If it drags, switch ANTHROPIC_MODEL to claude-haiku-4-5 for grading only. `ask` uses ANTHROPIC_ASK_MODEL (default claude-sonnet-5, low effort, ~2–4 s; set claude-haiku-4-5 to shave a second). A question asked *while a question is open* costs both calls back to back (classify+grade, then ask), ~6–8 s: "One sec." then "Nearly there." cover it, but watch it in rehearsal.
- The in-app browser blocks the microphone. `/learn/voice?text=1` runs the real agent without audio (it reads on its own; no text box on the Dial, it is hands-free), or use Chrome for the mic. For typed commands use Read mode.
- Restart `npm run dev` after editing .env.local; Next does not hot-reload env.
- Study read-aloud: the word highlighter is a rAF loop that only the `<audio>` `play` event starts. Anything that re-binds the listener effect in `useReadAloud.ts` mid-playback cancels the loop, so `loadBlock`'s deps must stay stable (speed lives in a ref) and the effect resumes the loop itself if the element is already playing. Fixed 2026-09-18 after the speed pill froze the highlight.
- Client tools (browser) vs webhook tools (dashboard): the component uses client tools so localhost works without ngrok. Configure them as type Client in the dashboard.

## Costs (tracked 2026-09-18)
- ElevenLabs: first 71 s voice call = 597 credits, LLM passthrough $0.015. Budget ~500 credits/min of conversation. Check with the script in scratchpad or the dashboard's Conversations tab.
- Anthropic: ingest ~ $0.08 per run (3.7k in / 7.1k out on Sonnet 5). Each open-answer grade ~ $0.002; now logged to the dev console as `[grade] …`.
- Nothing runs unless someone is talking to it. Idle costs zero.

## Speech pickup tuning (applied via configure-agent.ts)
- ASR: Scribe v2 Realtime, quality high, 18 keyword boosts (commands + SA finance terms).
- Turn detection: turn_v3, eagerness eager, speculative turn on.
- VAD background voice filter on (radio, passengers).
- Backchannel words ("mm", "okay", "yeah") do not trigger barge-in.
- Untested levers left: `turn_eagerness: "patient"` if it cuts you off; headset mic in the car.
- **Local barge-in ducking** (`src/components/useBargeInDucking.ts`): the server takes ~0.5–1 s to confirm an interruption (needs transcribed words, then the WebRTC playout buffer drains; the SDK's client-side interrupt is a no-op on WebRTC). We watch the local mic meter while the tutor is talking and drop output volume to 10% after 150 ms of speech-band energy above the adaptive noise floor. Server `interruption` event → volume 0 until the next agent utterance. No confirmation within 1 s → fade back (false alarm: a bump or a passenger costs a ~1 s dip, nothing else). Constants at the top of the hook. Open `/learn/voice?debug=1` to see `in · floor · ratio · out · PHASE` while calibrating. Needs a live spoken test with speakers, not headphones, so echo cancellation is exercised.
